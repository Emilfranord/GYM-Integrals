float tests(float j) {
  //tests of the math part of the function. 
  // no longer functional

  if (j>0 && j<13.0) {

  //  return -j*j;
  // can it integrate negative numbers: yes

  //  return 5;
  // can it do constant functions: yes

  //
  // can it do empty function: yes

  //  return j*j-6*j+5;
  // can it do integral or area: integral, y under 0 subtracts from the value.

  //  return j*j;
  // can it do x less than 0: no

  //  return pow(j, 6)+1000;
  // can it do higher exponents: yes

  //  return pow(j, 7);
  // can it do higher exponents: yes


  //  return pow(j, -2);
  // can it do negative exponents: no

  //  return sin(j);
  //  return cos(j);
  // return tan(j);
  // can it do trig: no, sin and cos can get processed, but not tan.  

  //  return pow(3, j);
  //  return pow(4, j);
  //  return pow(5, j);
  //  return pow(6, j);
  // can it do potens functions: yes

  //  return pow(j, 0.5);
  //  return pow(2*j, 0.5);
  // can it do square roots: yes


  //  return exp(j);
  // can it do natural exponents: yes
  
  //  return log(j);
  // can it do natural logs: yes
  }
  return 0;
}
